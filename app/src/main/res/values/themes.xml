<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Minimal theme - only android: attributes so AAPT never fails on missing library attrs -->
    <style name="Theme.TaizouInjector" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:colorPrimary">#8D00FF</item>
        <item name="android:colorPrimaryDark">#6B00CC</item>
        <item name="android:colorAccent">#00FFEE</item>
        <item name="android:colorBackground">#05080D</item>
        <item name="android:windowBackground">#05080D</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>

    <style name="Theme.TaizouInjector.Fullscreen" parent="Theme.TaizouInjector">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

    <style name="Theme.TaizouInjector.Overlay" parent="Theme.TaizouInjector">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Simple button style - no library parent -->
    <style name="Button.Oval" parent="android:Widget.Button">
        <item name="android:background">@drawable/btn_oval</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:textSize">12sp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
    </style>

    <!-- Simple SeekBar style - no library parent -->
    <style name="SeekBar.Custom" parent="android:Widget.SeekBar">
        <item name="android:maxHeight">6dp</item>
        <item name="android:minHeight">6dp</item>
        <item name="android:thumb">@drawable/seekbar_thumb</item>
        <item name="android:progressDrawable">@drawable/seekbar_progress</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

</resources>
